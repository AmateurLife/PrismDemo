<Window x:Class="PrismDemo.APP.Views.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="密码验证" Height="200" Width="350"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="请输入管理员密码：" FontSize="14" Margin="0,0,0,10"/>

        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Height="28" Margin="0,0,0,10" KeyDown="PasswordBox_KeyDown"/>

        <TextBlock x:Name="ErrorText" Grid.Row="2" Text="密码错误！"
                   Foreground="Red"
                   FontSize="10"
                   Visibility="Collapsed"
                   Margin="0,0,0,10"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="取消" Width="75" Height="28" Margin="0,0,10,0"
                    Click="CancelButton_Click" Cursor="Hand"/>
            <Button Content="确认" Width="75" Height="28"
                    Click="ConfirmButton_Click" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
