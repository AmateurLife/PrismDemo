<UserControl x:Class="PrismDemo.APP.Views.HomePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <StackPanel Margin="16">
        <TextBlock FontSize="18" FontWeight="Bold" Text="Prism 动态模块框架演示" Margin="0,0,0,12" />
        <TextBlock Text="当前已动态加载的模块：" Margin="0,0,0,6" />
        <ItemsControl ItemsSource="{Binding Modules}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="0,2">
                        <TextBlock Text="{Binding Name}" Width="120" FontWeight="Bold" />
                        <TextBlock Text="{Binding State}" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>