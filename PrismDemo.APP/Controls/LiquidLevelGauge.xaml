<UserControl x:Class="PrismDemo.APP.Controls.LiquidLevelGauge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="240" d:DesignWidth="200">
    <Viewbox Stretch="Uniform">
        <Grid x:Name="LayoutRoot" Width="200" Height="240">
            <Grid.RowDefinitions>
                <RowDefinition Height="200"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>

            <!-- 表盘区域（默认 Bottom 模式：占满 Row 0 Col 0-1） -->
            <Grid x:Name="GaugeRoot" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                  Width="200" Height="200">

                <!-- 外圈辉光 -->
                <Ellipse x:Name="GlowEllipse"
                         StrokeThickness="4"
                         Fill="{x:Null}"
                         Stroke="#06B6D4">
                    <Ellipse.Effect>
                        <DropShadowEffect x:Name="GlowEffect"
                                          BlurRadius="12"
                                          ShadowDepth="0"
                                          Color="#06B6D4"
                                          Opacity="0.7"/>
                    </Ellipse.Effect>
                </Ellipse>

                <!-- 深色底圆 -->
                <Ellipse x:Name="BgEllipse"
                         Fill="#1A1A2E"
                         Margin="4"/>

                <!-- 液面填充（裁剪到圆形） -->
                <Grid Margin="4">
                    <Grid.Clip>
                        <EllipseGeometry Center="96,96"
                                         RadiusX="96"
                                         RadiusY="96"/>
                    </Grid.Clip>

                    <Rectangle x:Name="LiquidRect"
                               VerticalAlignment="Bottom"
                               Height="0">
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop x:Name="GradStart" Color="#10B981" Offset="0"/>
                                <GradientStop x:Name="GradEnd" Color="#059669" Offset="1"/>
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                </Grid>

                <!-- 百分比文本 -->
                <TextBlock x:Name="PercentText"
                           Text="0%"
                           FontFamily="Segoe UI"
                           FontWeight="Bold"
                           FontSize="32"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect BlurRadius="4"
                                          ShadowDepth="1"
                                          Opacity="0.5"/>
                    </TextBlock.Effect>
                </TextBlock>
            </Grid>

            <!-- 标签（默认 Bottom 模式：底部居中） -->
            <TextBlock x:Name="LabelText"
                       Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                       Text=""
                       FontFamily="Segoe UI"
                       FontSize="14"
                       Foreground="#94A3B8"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Grid>
    </Viewbox>
</UserControl>
