# PrismDemo 项目文件树

WPF + Prism 模块化工业监控上位机（演示版）

> 已排除 `.git`、`bin`、`obj`、`.vs`、`_rename`、`*.csproj.user` 等非源码文件。

```
PrismDemo/
├── PrismDemo.sln                             # Visual Studio 解决方案
├── Directory.Build.props                     # 全局 MSBuild 属性（NoWarn）
├── README.md                                 # 项目说明
├── 项目文件树.md                              # 本文件
├── .gitattributes / .gitignore
├── .vscode/launch.json
│
├── PrismDemo.APP/                            # ★ 主程序 (WPF EXE) — 应用外壳
│   ├── PrismDemo.APP.csproj
│   ├── App.xaml / App.xaml.cs                # DI 注册 / 模块扫描加载 / 全局异常
│   │
│   ├── Assets/Fonts/                         # iconfont.ttf、demo_index.html
│   ├── Resources/GMichnography.png           # 图片资源
│   │
│   ├── Views/                                # ▸ 视图层
│   │   ├── MainWindow.xaml(.cs)              #   主窗口外壳（导航栏 + ContentRegion）
│   │   ├── HomePage.xaml(.cs)                #   首页看板（流量/余量卡片、实时趋势、合规率）
│   │   ├── ChartPage.xaml(.cs)               #   模块切换页（下拉选择 A/B 模块）
│   │   ├── ReportData.xaml(.cs)              #   数据报表 / CSV 导出
│   │   ├── HistoricalAlarm.xaml(.cs)         #   历史报警查询
│   │   ├── AlarmDetail.xaml(.cs)             #   报警详情
│   │   ├── Setting.xaml(.cs)                 #   系统设置（模块部署/热重载/数据库备份）
│   │   ├── SettingConnect.xaml(.cs)          #   采集点位配置
│   │   ├── SettingAlarm.xaml(.cs)            #   报警阈值配置
│   │   └── PasswordDialog.xaml(.cs)          #   设置口令校验
│   │
│   ├── ViewModels/                           # ▸ ViewModel 层 (MVVM)
│   │   ├── MainWindowViewModel.cs
│   │   ├── HomePageViewModel.cs
│   │   ├── ChartPageViewModel.cs
│   │   ├── ReportDataViewModel.cs
│   │   ├── HistoricalAlarmViewModel.cs
│   │   ├── AlarmDetailViewModel.cs
│   │   ├── SettingViewModel.cs
│   │   ├── SettingConnectViewModel.cs
│   │   └── SettingAlarmViewModel.cs
│   │
│   ├── Services/                             # ▸ 业务服务层
│   │   ├── APPMainService.cs                 #   主采集循环 (OPC → SQL Server)
│   │   ├── AlarmService.cs                   #   报警检测 / 去抖 / 弹窗
│   │   ├── DynamicModuleManager.cs           #   模块运行时加载/卸载/热重载 (ALC)
│   │   ├── ModuleDeploymentService.cs        #   模块部署（扫描/带时间戳复制 DLL）
│   │   ├── ModuleLoadContext.cs              #   自定义 AssemblyLoadContext
│   │   ├── ModuleContainerProvider.cs        #   模块子容器提供器
│   │   ├── ModuleSwitch.cs                   #   模块启停开关
│   │   ├── AppReloadService.cs               #   系统整体热重载
│   │   └── TimeSeriesBuffer.cs               #   时序环形缓冲
│   │
│   ├── Controls/                             # 自定义控件（进度条、液位计、加药图表）
│   └── Converters/                           # 值转换器
│
├── PrismDemo.Core/                           # ★ 共享内核 (类库)
│   ├── PrismDemo.Core.csproj
│   ├── CoreModule.cs                         # 内核模块（注册通用服务）
│   ├── core.config.json                      # 数据库/OPC/口令等全局配置
│   │
│   ├── Configuration/Config.cs               # 全局配置读取
│   ├── Interfaces/                           # ▸ 契约层
│   │   ├── IModuleSwitch.cs                  #   模块开关
│   │   ├── IModuleLifecycle.cs               #   模块生命周期
│   │   ├── IModuleDeploymentService.cs       #   模块部署
│   │   ├── IModuleContainerProvider.cs       #   子容器提供
│   │   ├── IReloadableModule.cs              #   可热重载模块
│   │   ├── IOpcService.cs / IWriteableService.cs
│   │   ├── IDatabaseService.cs / IAlarmConfigProvider.cs
│   │   ├── IErrorHandler.cs / IReportDataView.cs / IReportDataViewModel.cs
│   │   ├── IFilterConfig.cs                  #   字段滤波配置契约
│   │   └── ServiceProxy.cs                   #   热重载服务代理接缝
│   ├── Models/                               # SharedDataModel、ReadData、AlarmConfig 等
│   ├── Services/
│   │   ├── DatabaseService.cs                # SQL Server 访问（采集/报警/配置表）
│   │   ├── OpcServices.cs                    # OPC UA 读写与重连
│   │   ├── AlarmService 相关模型与运行时状态
│   │   ├── DataDownsampler.cs                # 曲线降采样
│   │   ├── WriteableBehavior.cs              # 可写点位行为
│   │   └── Log.cs / AlertLogger.cs / ErrorHandler.cs
│   ├── Controls/                             # GradientPercentProgressBar、LedIndicator、RingIndicator、WaterLevel_Arc
│   ├── Converters/                           # 通用值转换器
│   ├── Helpers/HistoryChartBaseViewModel.cs  # 历史曲线基类（查询/导出）
│   └── Views/                                # AlarmWindow、InputDialog
│
├── PrismDemo.A/                              # ★ 业务模块 A（模块键 "A"）
│   ├── PrismDemo.A.csproj                    # 构建后复制 DLL+配置到 APP 的 Modules 目录
│   ├── AModule.cs                            # 模块入口
│   ├── a.config.json                         # 模块参数（上下限）
│   ├── Settings.cs
│   ├── Configuration/Config.cs
│   ├── Interfaces/IAService.cs
│   ├── Models/                               # AFieldConfig、HistoryPoint
│   ├── Services/
│   │   ├── ADataService.cs                   # 字段抽取 / 建表 / 落库
│   │   └── AMainService.cs                   # 控制主循环（算法区 #region 保留、实现已移除）
│   ├── ViewModels/                           # ChartViewModel + 4 控制点 + 4 历史曲线
│   └── Views/                                # Chart、ChartPage1~4、HistoryChart1~4
│
└── PrismDemo.B/                              # ★ 业务模块 B（模块键 "B"，与 A 同构）
    ├── PrismDemo.B.csproj
    ├── BModule.cs
    ├── b.config.json
    ├── Settings.cs
    ├── Configuration/Config.cs
    ├── Interfaces/IBService.cs
    ├── Models/                               # BFieldConfig、HistoryPoint、RealtimeRow
    ├── Services/
    │   ├── BDataService.cs                   # 字段抽取 / 建表 / 落库
    │   └── BMainService.cs                   # 控制主循环（算法区 #region 保留、实现已移除）
    ├── ViewModels/                           # 与 A 同构
    └── Views/                                # 与 A 同构
```

## 说明

- **模块解耦**：`PrismDemo.APP` 不引用 `PrismDemo.A` / `PrismDemo.B`，模块以 DLL 形式运行期加载。
- **模块命名**：部署到 `modules` 目录的 DLL 形如 `PrismDemo.A.202604031714.dll`，模块名取 A / B。
- **热重载**：容器内 `ServiceProxy<T>` 常驻，重载仅替换其 `Target`。
- **演示版**：业务算法细节与现场点位名已通用化，配置文件中的连接串/口令/端点为占位值。
- **A / B 模块算法区**：两个模块的 `AMainService.cs` / `BMainService.cs` 均保留
  `#region 算法逻辑实现` 及其子 region 的结构，但算法实现（数据预处理、反馈/反馈更新、
  加药量计算、联动回写）已移除，只留占位注释；两模块各删除以下 5 个文件：
  `Services/ProcessModel.cs`、`Services/Feedback.cs`、`Services/UnitRateConvert.cs`、
  `Services/LinearInterpolation.cs`、`Models/FeedbackCount.cs`。
  采集、落库、界面、历史曲线、报表等功能不受影响。
- **`DataFilter`**：数据滤波工具（`DataFilter.FilterMean` / `ClearHistory` / `ClearAllHistory`）已从
  `PrismDemo.Core` 删除；只保留配置契约 `PrismDemo.Core/Interfaces/IFilterConfig.cs`，
  由 `AFieldConfig` / `BFieldConfig` 实现（对应模块配置表的 `IsFilter` / `FilterLength` 列）。
